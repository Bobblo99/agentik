Read .claude/skills/customize/SKILL.md and follow it to personalize the
framework for this project. Read memory/domain.md and memory/conventions.md
first for existing context. Pick the mode (project rule / domain term /
project skill) that fits my request below. For a project rule, end by running
`pnpm check:framework` and confirming it is green.

Request: $ARGUMENTS
