# Every environment variable used in code MUST be declared here first
# (rules/00-verification.md). Copy to .env and fill in real values.
# .env is gitignored — never commit secrets.

# EXAMPLE_API_URL=
# EXAMPLE_API_KEY=
